from django.urls import path
from . import views

app_name = 'reportes_dinamicos'

urlpatterns = [
    # Define aquí tus URLs de reportes
    # path('generar/', views.ReporteView.as_view(), name='generar'),
]

