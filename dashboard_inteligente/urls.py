from django.urls import path
from . import views

app_name = 'dashboard_inteligente'

urlpatterns = [
    # Define aquí tus URLs del dashboard
    # path('metricas/', views.MetricasView.as_view(), name='metricas'),
]

